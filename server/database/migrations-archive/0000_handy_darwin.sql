-- Migration de synchronisation : toutes les tables existent déjà
-- Ce fichier sert uniquement à enregistrer l'état actuel dans __drizzle_migrations

SELECT 1; -- Requête vide pour que la migration soit valide
